---
title: 'Listar pipeline stages configurados'
openapi: 'GET /integrations/v1/pipeline-stages'
---

<Note>
  Requiere header `x-api-key` y `tenantId` en query. Puedes filtrar por `pipelineId` opcionalmente.
</Note>
