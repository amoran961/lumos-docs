---
title: 'Listar oportunidades'
openapi: 'GET /integrations/v1/opportunities'
---

<Note>
  Requiere header `x-api-key` y `tenantId` en query. Puedes filtrar por `pipelineStageId` para traer oportunidades de una etapa específica del pipeline.
</Note>
